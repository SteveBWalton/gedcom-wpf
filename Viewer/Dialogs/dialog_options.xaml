<Window x:Class="gedcom.viewer.DialogOptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gedcom.viewer"
        mc:Ignorable="d"
        Title="DialogOptions" Height="450" Width="800" Loaded="windowLoaded" WindowStartupLocation="CenterOwner">
    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="5" Padding="5" IsCancel="True" Width="100" Height="31">Cancel</Button>
                <Button Margin="5" Padding="5" IsDefault="True" Click="buttonOkClick" Width="100" Height="31">OK</Button>
            </StackPanel>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="Show Gedcom" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0" />
            <CheckBox Grid.Column="1" Grid.Row="0" x:Name="_chkShowGedcom" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0,0,0" />
        </Grid>
    </DockPanel>
</Window>
